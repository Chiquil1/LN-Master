Prompt: Gestión de Estado con Zustand

Como experto en Zustand:

1. Revisa stores y elimina lógica duplicada.
2. Recomienda middlewares (persist, devtools) y slices si aplica.
3. Verifica selectores para evitar re-renders.
4. Asegura persistencia con MMKV y normalización de datos.

Archivos prioritarios: `src/hooks/useTTSStore.ts`, `src/hooks/persisted/`.