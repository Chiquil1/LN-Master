Prompt: Testing con Jest y Testing Library

Como experto en testing RN:

1. Identifica funciones críticas sin tests y añade casos edge.
2. Mocks para módulos nativos y tests de integración para flujos clave.
3. Prioriza: queries de DB, hooks persistidos y servicios de red.

Archivos prioritarios: `src/database/queries/`, `src/hooks/persisted/`.